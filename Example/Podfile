source 'https://github.com/CocoaPods/Specs.git'

target 'MultiStateButton', :exclusive => true do
  pod "MultiStateButton", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "MultiStateButton", :path => "../"

  pod 'Specta', '~> 0.2.1'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
